# Note: Vault initialization secrets should be managed manually
# This file is for development/testing purposes only
# In production, Vault initialization should be done manually with proper key management

apiVersion: v1
kind: Secret
metadata:
  name: vault-init
type: Opaque
stringData:
  # These are placeholder values - actual initialization should be done manually
  vault-init: "false"  # Set to "true" after manual initialization
  # vault-unseal-key-1: ""  # Unseal keys should be stored securely outside the cluster
  # vault-unseal-key-2: ""  # Unseal keys should be stored securely outside the cluster
  # vault-unseal-key-3: ""  # Unseal keys should be stored securely outside the cluster
  # vault-root-token: ""    # Root token should be stored securely outside the cluster