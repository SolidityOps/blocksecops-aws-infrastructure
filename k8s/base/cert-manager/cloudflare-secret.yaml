apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-api-token-secret
  namespace: cert-manager
  labels:
    app.kubernetes.io/name: cert-manager
    app.kubernetes.io/component: dns-solver
type: Opaque
stringData:
  # This should be replaced with the actual Cloudflare API token
  # In production, this would be managed by external-secrets or sealed-secrets
  api-token: "CLOUDFLARE_API_TOKEN_PLACEHOLDER"