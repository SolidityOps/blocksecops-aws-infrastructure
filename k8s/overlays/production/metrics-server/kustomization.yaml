apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: metrics-server-production

labels:
- pairs:
    app.kubernetes.io/instance: production-metrics-server
    environment: production
    team: devops

resources:
  - ../../../base/metrics-server
  - pdb.yaml

patches:
- path: deployment-patch.yaml