apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: eks-staging

labels:
- pairs:
    app.kubernetes.io/instance: staging-eks
    environment: staging
    team: devops

resources:
  - cluster-autoscaler/
  - metrics-server/
  - ebs-csi-driver/
  - aws-load-balancer-controller/
  - cert-manager/
  - external-secrets/
  - postgresql/
  - vault/
  - prometheus/