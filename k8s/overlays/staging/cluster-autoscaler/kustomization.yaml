apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: cluster-autoscaler-staging

labels:
- pairs:
    app.kubernetes.io/instance: staging-cluster-autoscaler
    environment: staging
    team: devops

resources:
  - ../../../base/cluster-autoscaler

patches:
- path: deployment-patch.yaml
- path: configmap-patch.yaml
- path: serviceaccount-patch.yaml