apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: metrics-server-staging

labels:
- pairs:
    app.kubernetes.io/instance: staging-metrics-server
    environment: staging
    team: devops

resources:
  - ../../../base/metrics-server

patches:
- path: deployment-patch.yaml