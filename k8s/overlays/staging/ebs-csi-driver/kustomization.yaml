apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: ebs-csi-driver-staging

labels:
- pairs:
    app.kubernetes.io/instance: staging-ebs-csi-driver
    environment: staging
    team: devops

resources:
  - ../../../base/ebs-csi-driver

patches:
- path: serviceaccount-patch.yaml